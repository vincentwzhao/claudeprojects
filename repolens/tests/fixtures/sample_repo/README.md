# Sample Repo

A tiny Express + JWT authentication API, used as a fixture repository for
RepoLens's test suite (tools, analyzer, and agent behavior against a real,
small, layered codebase).

Layers: `routes` -> `controllers` -> `services` -> `repositories` -> `db`.
